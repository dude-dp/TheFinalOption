{
  "name": "the-final-option",
  "version": "2.0.0",
  "private": true,
  "description": "TheFinalOption — Serverless MTF Screener (Cloudflare + Supabase)",
  "workspaces": [
    "cloud"
  ],
  "scripts": {
    "dev":         "cd cloud && npx wrangler dev",
    "deploy":      "cd cloud && npx wrangler deploy",
    "tail":        "cd cloud && npx wrangler tail",
    "type-check":  "cd cloud && npx tsc --noEmit",
    "db:migrate":  "cd cloud && npx wrangler d1 execute thefinaloption-db --remote --file=./migrations/system_logs.sql",
    "db:migrate:local": "cd cloud && npx wrangler d1 execute thefinaloption-db --local --file=./migrations/system_logs.sql"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}

